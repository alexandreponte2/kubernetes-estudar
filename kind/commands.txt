brew install podman
podman machine init --cpus 2 --memory 2048 --rootful
podman machine start

KIND_EXPERIMENTAL_PROVIDER=podman kind create cluster



kind create cluster --name goku01 --config clustergoku.yml



*criar o cluster com base em yml
KIND_EXPERIMENTAL_PROVIDER=podman kind create cluster --name goku01 --config clustergoku.yml

NAME                   STATUS   ROLES           AGE   VERSION
goku01-control-plane   Ready    control-plane   15m   v1.25.3
goku01-worker          Ready    <none>          14m   v1.25.3
goku01-worker2         Ready    <none>          14m   v1.25.3




*listar os contexts
kubectl config get-contexts

*mudar de contexts
kubectl config use-context kind-goku01


*remover cluster
KIND_EXPERIMENTAL_PROVIDER=podman kind delete cluster
